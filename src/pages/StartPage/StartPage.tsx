import { FC } from "react";

import { StartPageContainer } from "./StartPage.styled";

const StartPage: FC<{}> = (): JSX.Element => {
  return <StartPageContainer>Start Page</StartPageContainer>;
};

export default StartPage;
